syntax = "proto3";

package com.spotify.custom;

// Force the outer class name to something else (e.g., "SessionIdMessageProto")
option java_outer_classname = "SessionIdMessageProto";

message SessionIdMessage {
  string session_id = 1;
}
